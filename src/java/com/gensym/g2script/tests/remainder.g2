rem (x: float, y: float, z: integer)
w :float;
b,c :integer;
q :quantity;
begin
	w = remainder(x, y);
	w = remainder(b, c);
	w = remainder(x, c);
	w = remainder(b, y);
	w = remainder(q, c);
	w = remainder(q, x)
end
