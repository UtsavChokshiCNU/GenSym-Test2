# Definitions that are common to all the database bridges
# -------------------------------------------------------
GDBVER = V12_0r0
KB_VER = 834
GDBDIR = $(SRCBOX)/g2db/c
AUTHDR = $(SRCBOX)/g2db/c/authorize
SPECFC = ${SRCBOX}/specific/${PLATFM}
GSIVER = 12.0
