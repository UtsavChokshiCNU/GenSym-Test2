/* This is one of a few dummy include files that exist merely so that the
 * corresponding .c file will have a .h file.  Though not necessary, the file
 * provides greater regularity of structure, thereby simplifying the
 * makefile, allowing freer use of default rules, etc.
 */
